import React from 'react';

/**
 * Pure-CSS rotating cube spinner — exact replica of the desktop app's BoxSpinner.
 * The animation is defined in desktop.css (`.sim-app .loader`).
 */
const BoxSpinner = () => <span className="loader" aria-hidden />;

export default BoxSpinner;
